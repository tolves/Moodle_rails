<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
      <li class="nav-item active">
        <%= button_to :Home, :root , method: :get, class: 'btn btn-primary'%>
      </li>
      <li class="nav-item ml-2">
        <%= button_to :Back, :back , method: :get, class: 'btn btn-primary'%>
      </li>
      <% if current_user %>
      <li class="nav-item ml-2">
        <%= button_to 'My Account', user_path(current_user) , method: :get, class: 'btn btn-primary'%>
      </li>
      <li class="nav-item ml-2">
        <%= button_to 'Sign Out', :destroy_user_session , method: :delete, class: 'btn btn-primary'%>
      </li>
      <% end %>
    </ul>
  </div>
</nav>