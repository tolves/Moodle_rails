<%= form_for @authority, url: admin_role_role_authorities_path(@role), method: :post do |f| %>
  <%= hidden_field_tag :role_id, params[:role_id] %>
  <div class="field">
    <%= f.label :controller_name %> : <br/>
    <%== f.select :controller_name, @controllers, prompt: "Pick a Controller" %>
  </div>
  <br/>

  <div class="field">
    <%= f.label :action_names %>
    <div id="action_names">
    </div>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit data: {"disable-with": "Saving..."} %>
  </div>
<% end %>